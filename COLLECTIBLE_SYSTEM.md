# COLLECTIBLE SYSTEM SPECIFICATION — HERO OF ETERNIA (PROMPT 25)

## System Overview
`CollectibleDatabase.cs` manages collection progress and lore entries for relics, scrolls, statues, music records, and creature index entries.

## Persistence
Discovered collectible IDs are persisted in `SaveProfile` Version 20 via `ExplorationContentSaveData`.
